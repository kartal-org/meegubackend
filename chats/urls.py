from django.urls import path
from .views import *

urlpatterns = [
    # path("", MessageAPIView.as_view()),
    # # path("fetchMessage", GetMessagesAPIView.as_view()),
    # path("createRoom", CreateMessageRoomView.as_view()),
    # path("fetchRoom", GetMessageRoomView.as_view()),
    # path("sendMessage", SendMessageView.as_view()),
    # path("fetchMessage/<int:pk>", GetMessagesView.as_view()),
]
